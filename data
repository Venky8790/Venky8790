$array = @()
$n = 5
for($i=0;$i -lt $n; $i++)
{
$input = (Read-Host "enter data ")
if($input -ne ""){$array += $input}
}
$array|sort
$array = @()
for($i=0;$i -lt $n; $i++)
{
$input = (Read-Host " ")
if($input -ne ""){$array1 += $input}
}
$array1|sort

$x=0
foreach($array in $array1)
{
if($array -gt $array1)
{
$x++
}
}
